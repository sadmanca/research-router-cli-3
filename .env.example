# =============================================================================
# API Configuration (choose one or more)
# =============================================================================

# OpenAI Configuration
OPENAI_API_KEY=your-openai-api-key-here

# Gemini API Key (preferred for GenKG)
GEMINI_API_KEY=your-gemini-api-key-here

# Azure OpenAI Configuration (alternative to OpenAI)
# AZURE_OPENAI_ENDPOINT=https://your-resource-name.openai.azure.com/
# AZURE_OPENAI_API_KEY=your-azure-api-key-here

# =============================================================================
# Query Configuration
# =============================================================================

# Include text chunks in query context (default: false)
# Set to 'true' to include original text chunks along with graph data
# Set to 'false' to use only graph structure (entities, relations, communities)
INCLUDE_TEXT_CHUNKS=false

# Valid values: true, false, 1, 0, yes, no, on, off
# 
# Examples:
#   INCLUDE_TEXT_CHUNKS=false  # Pure graph-based retrieval (recommended for testing)
#   INCLUDE_TEXT_CHUNKS=true   # Graph + original text chunks
#
# Note: Can be overridden per-query with --with-chunks or --no-chunks flags
# See TEXT_CHUNKS_CONFIG.md for detailed documentation